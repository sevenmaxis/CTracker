<h1>Sign in</h1>

<%= form_for(:session, :url => session_path) do |f| %>
  <%= render 'shared/fields', :f => f %>
  <div class="actions">
    <%= f.submit "Sign in" %>
  </div>
<% end %>

<div>or</div>

<h1>Sign up</h1>

<%= form_for(:user, :url => new_user_path) do |f| %>
  <%= render 'shared/fields', :f => f %>
  <div class="actions">
    <%= f.submit "Sign up" %>
  </div>  
<% end %>
